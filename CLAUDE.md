# ikeP ポートフォリオサイト — 要件定義 v3.0（現行・唯一の正）

> **このファイルが、このサイトの唯一の正式な要件です。**
> v1.0（5ページ構成・ティール基調）と v2.0（オフブラック×アンバー）は**破棄済み**。
> 実装はすでにv3です。過去版に合わせて戻さないこと。→ 経緯は `noah/DECISIONS.md` D-003

---

## 0. これは何か

いけP（長野県小諸市のフリーランス。AI×コミュニケーションのコンテンツ制作／
コピーライティング／AI画像生成）のポートフォリオ。**単一ページの静的サイト**。

**コンセプト**：ポートフォリオを *上質な季刊誌「ikeP通信」* として見せる。
世界観は **ラグジュアリー × 温かい × 洗練**。

- **目的**：ココナラ／クラウドワークス経由の依頼者と、note／Xから来た読者に
  「この人に頼みたい」と思わせる。
- **ターゲット**：AI活用に興味はあるが「難しそう」と感じている個人・小規模事業者。
- **立ち位置**：「AIに強い。でも、いちばん人間くさくいたい。」＝ **AI × 人間味**。

設計原理の調査ノートは [`RESEARCH.md`](./RESEARCH.md)、
UI仕上げ用の引き継ぎは [`HANDOFF.md`](./HANDOFF.md)、
更新手順は [`README.md`](./README.md)。

---

## 1. ページ構成（単一ページ）

和文の章題で構成する（英語見出しの乱発を避けるため）。

```
表紙（Hero） → 序（Statement） → 仕事（Services） → 作品集（Works）
→ 人（Profile） → 便り（Contact）＋ 販売中（Shop）
＋ 連載「今日の30秒」（日替わり）
```

**ファーストビュー3秒で「誰が・何をしてくれる人か」がわかること。**

---

## 2. デザインシステム（v3）

| 要素 | 現行の値 |
|---|---|
| **配色** | 温かいアイボリーの紙 × エスプレッソの墨 × **真鍮の一点差し** |
| **欧字・見出し・数字** | Fraunces |
| **和文見出し・リード** | しっぽり明朝 |
| **本文・UI** | Noto Sans JP |
| **レイアウト** | エディトリアル（誌面）。主役1点＋サイズ差のある支持点 |
| **モーション** | 静かなフェード・罫線の伸び。最小限 |

- **色・フォント・余白・角丸・影・モーションは、すべて `css/tokens.css` にCSS変数で一元管理。**
  見た目の変更は原則ここだけで完結させる（`style.css` に直書きを増やさない）。
- **レスポンシブ必須。スマホ第一。** 375px幅で横スクロールを出さない。

---

## 3. 技術要件

- **静的HTML/CSS/JS。フレームワーク不使用。ビルド不要。**
- **GitHub Pages 公開前提**（`kotesane22/ikep` のルート／`main` ブランチ／`/(root)`）
- 公開URL：`https://kotesane22.github.io/ikep/`
- OGP・favicon・meta 記述込み
- `fetch` でJSONを読むため、ローカル確認は簡易サーバー経由
  （`python3 -m http.server 8000`／`file://` 直開きは動かない）

---

## 4. データ分離（この設計が本体）

**文言・リンク・作品は、すべて `data/*.json` にある。HTMLには書かない。**

| ファイル | 中身 |
|---|---|
| `data/works.json` | 作品（`items` に1件追記するだけで増える。`date` 降順で自動整列） |
| `data/shop.json` | 販売リンク（`url` が空なら「準備中」表示・リンクにしない） |
| `data/contact.json` | 問い合わせ導線（同上） |
| `data/profile.json` | プロフィール本文・項目表・実績 |
| `data/habits.json` | 連載「今日の30秒」（**書くほど周期が伸びる**） |

`index.html` に残っているのはマウント点と固定要素のみ：
`[data-works]` / `[data-shop]` / `[data-profile-facts]` / `[data-profile-body]` / `[data-contact]`
**これらを削除・改名しない。** 描画側（`js/*.js`）の**エスケープを必ず維持する。**

**サイト内決済は作らない。外部リンクのみ。**

---

## 5. 禁止事項（デザインガードレール）

- 紫グラデ、ありがちなグラデーションヒーロー
- 絵文字装飾
- 無意味な英語見出しの乱発
- 「羅針盤」「設計図」系のテンプレ比喩
- ソシャゲ風テンプレ、意味のない装飾
- **全セクション均一密度**（メリハリのない画面）

> **余白で見せる。作品が主役、装飾は脇役。**

---

## 6. 壊してはいけない安全機構

- `.reveal` の `.has-js` ガード（**JS無効でも内容が読める**）
- 連載の**日替わり決定性**：ローカル日付の通日 % 件数 で選ぶ（`js/habit.js`）。
  **`Math.random()` や `localStorage` を持ち込まない**（同じ日は同じ回が出ること）
- 375px幅で `document.scrollWidth === innerWidth`

---

## 7. 出す前の自己チェック

- [ ] 一目で「AI×人間味」の独自ポジションが伝わるか
- [ ] テンプレ／どこかで見たLP感がないか
- [ ] ファーストビュー3秒で「誰が・何をしてくれる人か」わかるか
- [ ] スマホで読みやすいか
- [ ] 禁止事項（§5）に触れていないか

---

## 8. 運用ルール

- **作品を増やす** ＝ `data/works.json` に1オブジェクト追記するだけ
- **販売リンクを増やす** ＝ `data/shop.json` に1オブジェクト追記するだけ
- **HTML/JSには手を入れない設計を厳守する**
- **デザイン変更は原則 `css/tokens.css` の変数だけで完結させる**
- 詳細な手順は `noah/workflows/portfolio-update.md`

### 公開について（重要）

**`main` へのマージ＝公開は、いけPの承認が必要。** ノアは作業ブランチまで。
GitHub Pages の有効化は手動操作が必要（自動化ワークフローは権限不足のため
コミット `5a0f422` で撤去済み）。

---

## 9. ノア運用OS（`noah/`）

このリポジトリには、いけPの専属AIパートナー「ノア」の運用台帳が同梱されている。
**セッションを始めたら `noah/START_HERE.md` を読むこと。**

| ファイル | 役割 |
|---|---|
| `noah/START_HERE.md` | 入口・全体地図・単一出典の原則 |
| `noah/USER_PROFILE.md` | いけPの人物・声のトーン・NG |
| `noah/GOALS.md` | 目標（北極星／90日／今週） |
| `noah/PROJECTS.md` | 進行中プロジェクト |
| `noah/TASKS.md` | **いまやること**（優先順位順） |
| `noah/APPROVALS.md` | 承認ルール・承認待ちキュー |
| `noah/DECISIONS.md` | 決定事項・未確定事項 |
| `noah/LEARNINGS.md` | 学び・未達の原因 |
| `noah/routines/` | 朝・昼・夜・週次 |
| `noah/workflows/` | 繰り返し作業の型 |

### 絶対に守る境界線

> **明示的な承認なしに、投稿・送信・公開・購入・契約・削除・アカウント設定変更は行わない。**

### ⚠️ `noah/` の公開に関する注意

**このリポジトリは GitHub Pages で全世界に公開されている。**
`noah/` を `main` にマージすると、目標・単価・プロフィールが
`https://kotesane22.github.io/ikep/noah/GOALS.md` のように誰でも読める状態になる。
**プライベートリポジトリへの移設が推奨**（→ `noah/DECISIONS.md` D-001）。
それまで、機微情報（金額・単価・個人連絡先）を台帳に書かないこと。

---

## 付記：バージョン履歴

| 版 | 内容 | 状態 |
|---|---|---|
| v1.0 | 5ページ構成（トップ/About/Works/Shop/Contact）・ティール基調・Noto Serif JP / Playfair Display | 破棄 |
| v2.0 | 単一ページ・オフブラック × アンバーゴールド | 破棄 |
| **v3.0** | 単一ページ・アイボリー紙 × エスプレッソ × 真鍮・Fraunces / しっぽり明朝 / Noto Sans JP | **現行** |

**過去版は履歴であり、参照先としては無効。** 実装（v3）が正。
